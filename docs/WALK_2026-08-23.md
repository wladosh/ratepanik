# Prod walk — 2026-08-23

PhoneShell only. Target: https://ratepanik.vercel.app  
Repo main: `b6f2cb3`. **Prod alias is stuck on `09121cb`** (last successful Production deploy). Every later Production deploy failed (`79b22c5` … `b6f2cb3`). Local `next build` on main succeeds.

Accounts: Host `wl.udod+1@gmail.com`. Join: isolated guest (`/?join=CODE`), not `+2` (shared browser cookie jar).

Room this walk: **P4R37H**. Baseline host: Level 4, XP 170/250, Hirncoins 120, Streak 1.

---

## 1) Auth login + guest join `/?join=CODE`

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 1.1 | Email login (host) | **PASS** | `/` → Anmelden → `wl.udod+1@gmail.com` / `testtest` → Home “Wladi / Party-Spieler”. |
| 1.2 | Guest join `/?join=CODE` | **PASS** | Host created lobby `P4R37H`. Fresh context `https://ratepanik.vercel.app/?join=P4R37H` (code 6/6) → Beitreten → lobby as `Gast-URMM`. |

## 2) Lobby settings

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 2.1 | Host editors: theme mix, mode, difficulty, blocks, timer, guests | **PASS** | Lobby shows Inhalt/Form/Tempo/Raum. Set Nur Schätzfragen, 1 Block, 1 Frage/Block, 5s, Gäste an. |
| 2.2 | Guest summary chips only | **PASS** | Guest: chips `Themen: Zufall`, `Nur Schätzfragen`, `Mix`, `1 Blöcke`, `5s Timer`, `Max 4`, `Gäste ja`. CTA “Warten auf Host…” — no editors. |
| 2.3 | Host start uses settings | **PASS** | DB `rooms.settings`: `modeFilter=number_guess`, `blocks=1`, `questionsPerBlock=1`, `timerSeconds=5`, `allowGuests=true`. Match: “Block 1 von 1”, Zahlenraten, 5s pill. |
| 2.4 | find_lie / order_it filter | **FAIL** (prod) | Mode buttons “Lüge finden · Bald” / “Reihenfolge · Bald” **disabled**. Main has them enabled; not on this deploy. |

## 3) Match

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 3.1 | number_guess | **PASS** | Theme pick → “Wie viele Pokémon-Typen gibt es…”. Guest guessed 91. Correct 18. |
| 3.2 | pick_correct | **FAIL** (not walked) | Need a second match with “Nur Auswählen”. Not run this pass. |
| 3.3 | find_lie | **FAIL** (prod) | Filter stubbed Bald; cannot force mode. |
| 3.4 | order_it | **FAIL** (prod) | Same as 3.3. |
| 3.5 | Timer + last-answer | **PASS** | After guest submit: timer pill 3s→1s, footer `1/2 haben schon peinlich getippt` (did not reveal on first answer). Timeout → reveal (host never answered). Soft-hide = question stays until timeout/all-in, then reveal. |

## 4) Match-end rewards → Home XP/Hirncoins

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 4.1 | Rewards screen (host) | **FAIL** (optimistic) | UI: `+90 XP`, `+40` Hirncoins, “Level 5!”. Client `calculateMatchRewards` only. |
| 4.2 | Persist + Home update | **FAIL P0** | Home/Stats still Level 4, XP 170/250, Hirncoins 120. Stats “Spiele beendet 3” unchanged. SQL: no `match_rewards` row for `P4R37H`. Postgres `14:32:30Z`: `new row violates row-level security policy for table "match_rewards"`. Prod app (`09121cb`) **INSERTs** `match_rewards`; DB migration `008` dropped that INSERT policy. Main already uses `grant_match_rewards` RPC. |

## 5) Achievement toast (`first_room`)

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 5.1 | Toast on room create | **FAIL** (N/A this account) | Achievements: Gastgeber already “Freigeschaltet”. No toast on this create. Need a fresh host. |

## 6) Home (registered)

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 6.1 | Freunde list / last-seen / invite | **FAIL** (prod) | Freunde → “BALD / Freundesliste kommt später”. Main has list+invite; not on `09121cb`. |
| 6.2 | Shop buy/equip `default_01`–`06`, no lootbox | **FAIL** (prod) | Shop → “BALD / Shop kommt später” (Lootboxen Phase C copy). No catalog. |
| 6.3 | Stats | **PASS** | Level 4, XP 170/250, Hirncoins 120, Spiele 3, Siege 3, Tage-Streak 1. |
| 6.4 | Achievements list | **PASS** | Erster Sieg Freigeschaltet, Gastgeber Freigeschaltet, 3-Tage-Streak Noch nicht. |
| 6.5 | Streak number | **PASS** | Home 🔥 Streak **1**. |

## 7) Guest: no progression header; Shop/Freunde account CTAs

| # | Check | Result | Repro |
|---|--------|--------|-------|
| 7.1 | No XP/Hirncoins header as guest | **PASS** | Landing guest has no XP bar. Match-end: “Erstelle ein Konto, um XP & Hirncoins zu sammeln.” / Anmelden. |
| 7.2 | Shop/Freunde account CTAs | **FAIL** (prod) | Guest never reaches Home panels (landing unless `/?join=`). Those panels are placeholders even for host. Main has “Shop/Freunde brauchen ein Konto” + Anmelden. |

---

## P0

**No tiny PR.** Main already grants via RPC; prod app is the old INSERT path against locked RLS.

Unblock **Vercel Production** (failed since `79b22c5`; inspect `dpl_E5hJkKkmmv5XNE9VpGeyP8r6CSyw`) and promote **main**. That lands rewards RPC, shop, friends, find_lie/order_it.

Do **not** restore `match_rewards` INSERT policy — that undoes the economy lock.

---

## Follow-ups

1. Fix/redeploy Production to `b6f2cb3` (or later). Re-walk 4.2, 6.1, 6.2, 2.4, 3.2–3.4, 7.2.
2. pick_correct match after deploy.
3. first_room toast on a new account.
4. Dual-account friends invite (`+1` / `+2`) after Freunde ships.
5. Host missed the 5s question while still on theme pick — consider timer start after theme confirm, or min timer when picker is host.
6. Rewards UI should not show `+XP` until RPC `ok` (main still sets client totals first).
